# ToDo

Personal task management app on the PERN stack with TypeScript.

```sh
npm install
npm run db:up              # Postgres in Docker on host port 5433
npm run db:migrate
npm run dev                # server + client together
```

Open http://localhost:5173.
